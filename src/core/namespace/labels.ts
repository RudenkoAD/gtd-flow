/** User-facing aliases shared by MCP and the QuickJS widget. The two callers keep
 * different unknown-name policies (MCP errors, widget falls back), but recognised
 * aliases must not drift. */
export const COMMON_NAMESPACE_LABEL = "Общее";
export const ALL_NAMESPACES_LABEL = "Все";
