Bug-fix release; no migration and no vault changes are required.

- Settings no longer loop when a save handler is awaited twice.
- The AI provider shares the plugin clock instead of reading wall time on its own.
- The release script preserves the original JSON formatting of `manifest.json` and `versions.json`.
