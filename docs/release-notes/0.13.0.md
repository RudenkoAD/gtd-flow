**Breaking release:** runtime namespaces are replaced by one unified inbox and explicit task scopes; the plugin is now desktop-only and adds the opt-in AI Inbox MVP.

Back up the vault before upgrading. Legacy namespace vaults must use the built-in dry-run, apply, resume, and rollback migration commands. [Read the full upgrade and privacy guide](https://github.com/RudenkoAD/gtd-flow/blob/v0.13.0/docs/BREAKING_AI_INBOX_MVP.md).
